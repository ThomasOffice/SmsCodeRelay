# 通信协议

## 概述

Android App 通过蓝牙 SPP (RFCOMM) 向 PC 端推送验证码消息。
连接方向：Android 为客户端，PC 为服务端。

## 蓝牙参数

| 参数 | 值 |
|------|-----|
| 协议 | RFCOMM (SPP) |
| UUID | `00001101-0000-1000-8000-00805F9B34FB` |
| 模式 | 事件驱动，每条短信建立一次连接，发送后关闭 |
| 波特率 | 9600 (SPP 默认) |

## 消息格式

每条消息为一行 UTF-8 JSON，以 `\n` 结尾。

```json
{
  "v": 1,
  "token": "预共享密钥",
  "sender": "10086",
  "body": "【XX】您的验证码是 123456，5分钟内有效",
  "code": "123456",
  "ts": 1730000000000
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `v` | int | 协议版本，当前为 1 |
| `token` | string | 预共享密钥，PC 端校验，不匹配则丢弃 |
| `sender` | string | 短信发送方号码 |
| `body` | string | 短信完整正文 |
| `code` | string | App 端提取的验证码（可能为空，PC 端会再提取一次） |
| `ts` | long | 毫秒时间戳 |

## PC 端处理流程

1. 从 COM 端口按行读取 JSON
2. JSON 解析失败 → 记日志丢弃
3. token 校验失败 → 记日志丢弃
4. 提取验证码：优先 `code` 字段，为空则用配置规则从 `body` 提取
5. 去重：与上次相同 code 且在 `dedup_seconds` 内 → 忽略
6. 写入系统剪贴板 (`pyperclip.copy`)
7. 弹出 Windows 通知
8. 记录日志

## 安全

- token 为预共享密钥，App 与 PC 必须一致
- 蓝牙配对本身提供链路层加密
- PC 端仅监听蓝牙传入 COM 端口，不暴露网络端口
